apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-pvc
  namespace: pos-system
  labels:
    app: mongodb
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: managed-csi  # Azure managed disk
  resources:
    requests:
      storage: 10Gi  # Start small for cost optimization